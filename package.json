{
  "name": "reviewer",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "cd dashboard && bun install && bun run build && cd .. && bun run src/index.ts",
    "dev": "bun --watch src/index.ts & cd dashboard && bun install && bun run dev",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@mariozechner/pi-agent-core": "^0.66.1",
    "@mariozechner/pi-ai": "^0.66.1",
    "ai": "^6.0.156",
    "ai-sdk-provider-claude-code": "^3.4.4",
    "ai-sdk-provider-codex-app-server": "^1.1.7",
    "zod": "^4.3.6"
  }
}
